<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @note.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @note.content %>
</p>

<p>
  <strong>Courses:</strong>
  <%= @note.course_notes.map{|c| c.course.code} %>
</p>

<p>
  <strong>User:</strong>
  <%= @note.user.proper_name %>
</p>
<p>
<%= link_to "Add a new course to this note", new_course_note_path(note_id: @note.id) %>
</p>
<%= link_to 'Edit', edit_note_path(@note) %> |
<%= link_to 'Back', notes_path %>
