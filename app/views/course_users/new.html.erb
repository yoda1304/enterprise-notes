<%= form_with(model: @course_user, local: true) do |form| %>
  <% if @course_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course_user.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
        <% @course_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= form.label :course_id %>
    <%= collection_select(:course_user, :course_id, Course.all, :id, :code, prompt: true) %>
  </div>
  <div class="field">
    <%= form.hidden_field :user_id, value: @user.id %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>



